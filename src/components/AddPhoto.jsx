import React, { Component } from 'react'
import Title from './Title'

export default class AddPhoto extends Component {
  render() {
    return (
      <div>
        <Title title={'This is the page where we will add photos'} />
      </div>
    )
  }
}
